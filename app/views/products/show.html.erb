<div class="container center" id='product-header'>
  <p>
    <strong>Item:</strong>
    <%= @product.gusti_id %>
  </p>
  <p>
    <strong>Description:</strong>
    <%= @product.description %>
  </p>
  <p>
    <strong>Current:</strong>
    <%= @product.current %>
  </p>
  <p>
    <strong>Reorder in:</strong>
    <%= "#{@product.reorder_in} days" %>
  </p>
</div>

  <table id='toptable'>
    <h2><strong><u>Top 20 Customers in Last 6 Months</u></strong></h2>
    <tr>
      <th>Rank</th>
      <th>Customer</th>
      <th>Units Purchased</th>
    </tr>
    <% @top_twenty.each_with_index do |(customer, quantity), rank| %>
    <tr>
      <td><%= rank + 1 %></td>
	    <td><%= customer %></td>
      <td><%= quantity %></td>
    </tr>
    <% end %>
  </table>

<div class="container">
  <p>
    <%= pie_chart @top_twenty %>
  </p>
</div>


<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
