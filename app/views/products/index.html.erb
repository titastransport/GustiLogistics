<main class="row">
  <section class="col-md-9">
    <table class="table table-hover" id="products-list">
      <h1>Products</h1>
      <%= render 'products', collection: @products %>
    </table>
  <div class="apple_pagination">
    <%= will_paginate @products, :container => false %>
  </div>
  </section>
  <section class="col-md-3">
    <%= form_for :product, url: products_path, method: :get, html: { id: "products_search" }, remote: true do |f| %>
      <p>
        <%= f.label :term, 'Search Products' %>
        <%= f.text_field :term, name: :term, value: params[:term] %>
        <%= f.submit :search, name: nil %>
      </p>
    <% end %>
  </section>
</main>

<!-- <h3 id="add product"><%= link_to 'Add a New Product', new_product_path %></h3> -->
