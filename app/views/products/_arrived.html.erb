<%= form_for(@product, remote: true, authenticity_token: true, html: { class: "pull-right", id: "enroute-form" }) do |f| %>
  <%= f.hidden_field :enroute, :value => false %>
  <%= f.submit "Mark as Arrived", class: "btn" %>
<% end %>
